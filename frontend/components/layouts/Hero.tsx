export const Hero = () => {
    return (
        <div className="hero">
            <span className="hero-tag">PDF Slicer</span>
            <h1 className="hero-title">Extract the pages<br />you <em>actually</em> need</h1>
            <p className="hero-sub">Upload your PDF, choose the pages, and download a clean new document in seconds.</p>
        </div>
    )
}